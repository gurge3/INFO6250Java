export class User {

    constructor(public userId: number, public username: string, public roleName: string, public roleId: number, public disabled: boolean, public disableReason: string) {

    }

}
